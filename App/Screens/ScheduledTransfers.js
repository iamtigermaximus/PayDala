import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ScheduledTransfers = () => {
  return (
    <View>
      <Text>ScheduledTransfers</Text>
    </View>
  );
};

export default ScheduledTransfers;

const styles = StyleSheet.create({});
